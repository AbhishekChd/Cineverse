<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MoviePosterTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">@dimen/text_padding_vertical</item>
        <item name="android:paddingEnd">@dimen/text_padding_vertical</item>
        <item name="android:paddingTop">@dimen/text_padding_horizontal</item>
        <item name="android:paddingBottom">@dimen/text_padding_horizontal</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>
